[pytest]
# SHA-pinned submodules have their own test suites; collecting them here would
# (a) pollute the harness pytest run with sibling-skill tests and
# (b) clash on `tests/` package names with this repo's own test dir.
norecursedirs = skills_repo .git .venv output db_export
testpaths = tests
